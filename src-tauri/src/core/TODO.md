# `crate::core` TODOs

- [x] `loader`: Read dataset into raw `ndarray::{Array1,Array2}` and metadata
- [x] `filter`: Apply filtering on `loader::RawDataset` and store dropped indices
- [x] `session`: Store a light state with the `RawDataset` and the operations done with it.
- [ ] `ops`
  - [ ] Select variables accordingly
  - [ ] stream progress and errors
